import {RequestObject} from "@/models/chat-request/RequestObject";
import {ChatObject} from "@/models/chat-instance/ChatObject";

export type ModalWindow = {
    id: string,
    requestObj?: string
    chatObj?: string
}